<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Màu nền -->
    <corners android:radius="24dp" /> <!-- Bán kính bo tròn (có thể điều chỉnh theo ý muốn) -->
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Đường viền (tùy chọn) -->
</shape>