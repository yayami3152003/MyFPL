<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app"
        android:supportsRtl="true"
        android:theme="@style/Theme.FPOLY"
        tools:targetApi="31">
        <activity
            android:name=".metmoi.activity.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".metmoi.activity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".metmoi.activity.MyBottomSheetActivity"
            android:exported="false" />
        <activity
            android:name=".metmoi.activity.MainActivity"
            android:exported="false"/>
        <activity
            android:name=".metmoi.activity.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>